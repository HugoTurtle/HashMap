# HashMap
A custom implementation of a HashMap in JavaScript using separate chaining with linked lists for collision handling.

This project demonstrates core data structure concepts including hashing, collision resolution, dynamic resizing, and key-value storage.